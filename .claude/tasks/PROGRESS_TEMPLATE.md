# 進捗引き継ぎノート — <プロジェクト名 / 機能群>

> **用途**: 複数セッション(複数コンテキストウィンドウ)にまたがる作業の引き継ぎ。
> このファイルをコピーして `output/tasks/PROGRESS.md` として保存する。
> SessionStart フックが冒頭を自動注入するので、次のセッションは推測なしで再開できる。
>
> **原則**(Anthropic の長時間エージェント運用知見):
> 1 セッション 1 機能 / 着手前にスモークテスト / 終了時はテスト緑 + コミット + 本ファイル更新。
> 機能リストの行は **`passes` 列を更新するだけ**。行の削除・受け入れ条件の書き換えは禁止
> (未実装の機能が「無かったこと」になるため)。

---

## 1. 現在の状態

| 項目 | 内容 |
| ---- | ---- |
| 最終更新 | YYYY-MM-DD HH:MM(session `xxxxxxxx`) |
| ブランチ | `feature/...` |
| 最後のコミット | `abc1234 feat: ...` |
| 起動コマンド | 例: `npm run dev` |
| スモークテスト | 例: `npm test -- --run smoke` → 期待: all pass |
| 既知の壊れている箇所 | なし / `...`(次セッションが最初に直す) |

---

## 2. 機能リスト(`passes` のみ更新)

| ID | 機能 | 受け入れ条件(E2E で確認できる粒度) | passes |
| -- | ---- | ---------------------------------- | ------ |
| F01 | <!-- 例: ユーザーがログインできる --> | <!-- 例: /login で正しい認証情報を入れると /dashboard に遷移する --> | ❌ |
| F02 | | | ❌ |

- `passes` は **実際に検証して初めて ✅**(ユニットテストだけでなく、ユーザー操作相当の E2E / 手動確認)
- 行を追加してよいのは要件が増えたときだけ。削除は禁止

---

## 3. 次にやること(優先順)

1. F01 — <!-- 最初の一手を 1 行で -->
2. F02 — ...

---

## 4. セッションログ(新しいものを上に)

### YYYY-MM-DD — session `xxxxxxxx`

- **やったこと**: ...
- **検証**: `npm test` → 42 pass / 0 fail(証拠を貼る)
- **コミット**: `abc1234 feat: ...`
- **未完了 / 申し送り**: ...
- **判断・却下した案**: ...
