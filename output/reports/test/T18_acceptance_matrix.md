# T18 受け入れ基準対応表

> 対応: `output/tasks/TASK_tadcap_mvp.md` T18 / PRD `output/prd/PRD_tadcap_mvp.md` §3(機能要件)・§4(非機能要件)。
> PRD の各 FR/NFR の受け入れ基準(`- [ ]` 箇条書き)を全数列挙し、各項目を以下の3区分に分類する。
>
> - **自動**: `npm run test:run`(Vitest)/ `cargo test` / `npm run e2e`(Playwright)のいずれかで機械的に検証済み(本レポート作成時点で全 exit 0)
> - **手動 #N**: `testreport/manual/CHECKLIST_T18.md` の項番 N に対応。**本レポート作成時点では人間による実施待ち**(チェックリストを用意した段階であり、実施・記録はこれから)
> - **未カバー**: 自動・手動のいずれにも独立した合否判定項目が無いもの(理由と対応案を付記)
>
> 実施日: 2026-09-24 / 自動検証結果: build 0 / vitest 0(128 tests, 13 files) / cargo test 0(61 passed) / clippy 0(warnings 0) / e2e 0(2 passed)

## FR-001: 範囲指定キャプチャ

| 受け入れ基準 | 区分 | 根拠 |
| ------------ | ---- | ---- |
| Rust側から`screencapture -i <出力パス>`を起動すること | 自動 + 手動#2 | `src-tauri/src/capture/screencapture.rs`(`ScreenCaptureCli`、cargo test で起動判断ロジックを検証)。実際の起動確認は手動#2 |
| 出力パスは一時ディレクトリ配下にキャプチャごとに一意なファイル名で作成されること | 自動 | `src-tauri/src/capture/tempfile.rs::generate_capture_path_は連続n回呼び出しても重複しない`(cargo test) |
| Escでキャンセルした場合はエラー扱いにせず、何もしないこと | 自動 + 手動#4 | `screencapture.rs::outcome_for_はファイル未生成ならcancelledを返す`(cargo test)。実機のEsc操作確認は手動#4 |
| 撮影後、画像がフロントエンドへ渡されCanvasに読み込まれること | 自動 + 手動#2 | `src/ipc/capture.test.ts`(`capture://completed`イベント購読)・`src/canvas/canvasState.test.ts`(画像セット純粋関数)。実際の表示確認は手動#2 |

## FR-002: ウィンドウキャプチャ

| 受け入れ基準 | 区分 | 根拠 |
| ------------ | ---- | ---- |
| `screencapture -i`起動中にスペースキーでウィンドウ選択モードへ切り替えられること | 手動#3 | OS標準機能(`screencapture`自体の挙動)のため自動化不可 |
| FR-001と同じ一時ファイル命名規則・キャンセル時の挙動に従うこと | 自動 | FR-001と実装共通(`capture::run()`)。`Capture.kind`は設計決定によりMVPでは常に`"range"`固定(ゲート2決定#2、`output/tasks/PROGRESS.md`参照)であり、Window/Rangeの分岐自体を持たない |

## FR-003: 全画面キャプチャ(MVP外)

対象外(PRD §3 FR-003が「本PRDでは受け入れ基準を定義しない」と明記)。

## FR-004: グローバルショートカット

| 受け入れ基準 | 区分 | 根拠 |
| ------------ | ---- | ---- |
| 他アプリがアクティブな状態でも既定キー押下でキャプチャが起動すること | 手動#9 | OSグローバルショートカットの実配送はOS依存のため自動化不可。押下判定ロジック自体(`should_handle_shortcut_event`)は`src-tauri/src/shortcuts.rs`のcargo testでカバー |
| 既定キーは`Cmd+Shift+4`を避け`Cmd+Shift+2`とし、実装時にOS標準・主要アプリとの衝突を確認したうえで確定すること | 自動 + 手動#10 | `shortcuts.rs::default_capture_shortcut_はcmd_shift_2で構成される`/`他のキー組み合わせにマッチしない`(cargo test、Apple公式サポート文書によるキー衝突なしの一次確認済み)。手元の主要アプリでの最終衝突確認は手動#10 |
| ショートカットキーの変更UIは提供しない(MVP外) | 未カバー(独立項目なし) | `src/ui/toolbar.ts`等にキー変更要素が無いことをgrep・コードレビューで確認済み(本タスクで再確認、該当UI要素なし)だが、これを継続的に検証する自動/手動チェック項目は無い。将来UI変更時のリグレッション防止には簡単なDOM不在テストの追加が有効(本タスクのスコープ外、報告事項として記録) |
| 押下後、FR-001と同じ`screencapture -i`が起動すること | 手動#9 | FR-004固有の実起動確認 |
| 撮影完了後、エディタウィンドウ(メインウィンドウ)が最前面に表示されること | 手動#9 | OSのウィンドウ前面化はOS依存のため自動化不可 |
| Escでキャンセルした場合はFR-001と同様エラー扱いにしないこと | 自動 + 手動#4 | FR-001と実装共通 |

## FR-005: クリップボードへコピー

| 受け入れ基準 | 区分 | 根拠 |
| ------------ | ---- | ---- |
| 目立つ位置に「クリップボードにコピー」ボタンが配置されていること | 手動#21(目視) | UI配置の目視確認。ボタンの有効/無効ロジックは`src/ui/clipboardButton.test.ts`で自動検証済み |
| ショートカット`Cmd+C`でコピーできること | 自動 + 手動#22 | `clipboardButton.test.ts`(Cmd+C判定・テキスト入力中は奪わないガード)。実機での実挙動確認は手動#22 |
| Canvas上の最終画像(PNG)がmacOSのクリップボードへ直接書き込まれること | 手動#21・#22 | 実クリップボードへの書込はOS依存のため自動化不可。実際に他アプリへ貼り付けて確認する |
| `@tauri-apps/plugin-clipboard-manager`での書き込みに失敗した場合、Rustコマンドによるフォールバックへ切り替わること | 自動(分岐ロジック) / **未カバー(実クリップボードでの実地確認)** | `src/ipc/clipboard.test.ts`(プラグイン失敗をモックしフォールバックへ切替わることを検証)・`src-tauri/src/clipboard/mod.rs::validate_rgba_*`(フォールバック側の入力バイト列型検証)。**実クリップボードで実際にフォールバック経路を発火させて書込確認する手段がUI上に無いため、`testreport/manual/CHECKLIST_T18.md`項目23で明示的に対象外とした。既知のカバレッジギャップとして次段落に記載** |

## FR-006: 注釈: 矢印

| 受け入れ基準 | 区分 | 根拠 |
| ------------ | ---- | ---- |
| ドラッグ操作で始点から終点への矢印がCanvasに描画されること | 自動(座標計算) + 手動(#17・#21操作時に目視) | `src/canvas/tools/arrowTool.test.ts`(座標計算純粋関数)。実際の描画結果はDOM依存のため自動テスト対象外、チェックリスト#17・#21実施時に目視で確認できるが独立の合否項目は設けていない |
| 既定色は視認性の高い鮮やかなピンク`#FF5C8A`であること | **未カバー(独立項目なし)** | `src/styles.css:30`(`--arrow-color: #ff5c8a`)に定数として固定されていることをコードで確認済み。DOM依存のため自動テスト対象外(`arrowTool.ts`コメントに明記)であり、レンダリング結果の色を検証する手動チェック項目も用意していない(低リスクと判断したが、厳密には未カバー) |
| 矢印ツールとモザイクツールをトグルで切り替えられること | E2E | `e2e/capture-flow.spec.ts`(ツール切替→矢印描画→モザイク適用のUIフローを検証、Playwright chromium) |

## FR-007: 注釈: 枠・テキスト(MVP外)

対象外(PRD §3 FR-007が「本PRDでは受け入れ基準を定義しない」と明記)。

## FR-008: モザイク

| 受け入れ基準 | 区分 | 根拠 |
| ------------ | ---- | ---- |
| ドラッグで矩形領域を選択できること | 自動 | `src/canvas/tools/mosaicTool.test.ts`(ドラッグ方向正規化・Canvas境界クリップの各パターン) |
| 選択領域がCanvasのピクセルに焼き込まれること(CSSフィルタ等は不可) | 自動 + E2E | `mosaicTool.test.ts`(ブロック平均化・端数ブロック処理・入力配列不変性の純粋ロジック検証)。ピクセル処理として実装されている(CSSクラス切替ではない)ことをコードで確認。E2Eでも適用操作自体を検証 |
| 矢印ツールとモザイクツールをトグルで切り替えられること | E2E | FR-006と同一根拠 |
| 加工方式はピクセル化のみとし、ぼかしオプションは実装しないこと | レビュー確認(グレップベース) | `src/canvas/tools/`配下に`arrowTool.ts`/`mosaicTool.ts`のみ存在し、blur実装が無いことを確認(本タスクでgrep実施、該当なし) |

## FR-009: メニューバー常駐

| 受け入れ基準 | 区分 | 根拠 |
| ------------ | ---- | ---- |
| アプリ起動時にメニューバーへアイコンが常駐すること | 手動#1 | トレイ構築(`src-tauri/src/tray.rs`)はcargo buildでコンパイル確認済みだが、実際の表示はOS依存のため手動確認 |
| メニューは「キャプチャ」「エディタを開く」「終了」の最小構成であること | 自動(変換ロジック) + 手動#13・#14・#16 | `tray.rs::tray_menu_action_from_id_は各バリアントへ変換する`(cargo test、id→アクション変換の純粋ロジック)。実際のメニュー表示・各項目動作は手動確認 |
| 「キャプチャ」選択でFR-001と同じキャプチャ操作が起動すること | 手動#13 | |
| 「エディタを開く」選択でメインウィンドウが表示され、前面に来ること | 手動#14 | |
| 「終了」選択でアプリケーションプロセスが終了すること | 手動#16 | |
| メインウィンドウを閉じてもアプリケーションプロセスは終了せず、メニューバー常駐とグローバルショートカットが有効なままであること | 手動#14・#15 | |
| (決定事項)Dockアイコンは非表示とする | 手動#1 | `app.set_activation_policy(Accessory)`実装済み。実際の非表示確認は手動 |

## FR-010: 履歴一覧(セッション内)

| 受け入れ基準 | 区分 | 根拠 |
| ------------ | ---- | ---- |
| サイドバー(リスト)にセッション中のキャプチャが表示されること | 自動 + E2E + 手動#17 | `src/history/historyStore.test.ts`(追加ロジック・先頭挿入順)。E2E(`capture-flow.spec.ts`)は1件表示・選択を検証。5件表示の実機確認は手動#17 |
| 項目クリックでその画像がCanvasに再読み込みされること | 自動 + E2E + 手動#18 | `historyStore.test.ts`(`selectHistoryItem`)。E2Eは直近1件の選択を検証。**複数件中の非最新項目**のクリック確認は手動#18で追加(既存の自動/E2Eテストは最新1件のみのため) |
| アプリ終了時に履歴が破棄されること(永続化しないこと) | 手動#20 | メモリ上のみの状態のため、実際のプロセス終了→再起動での確認は自動化不可 |
| 再読み込み時は編集後(マークアップ済み)画像を読み込むこと(元画像には戻さない) | 自動 + 手動#18 | `historyStore.test.ts::updateSelectedItemImageで選択中項目のimage/thumbnailが上書きされる`。実際のCanvas表示確認は手動#18 |

## NFR-001: キャプチャ起動レスポンス(200ms未満)

| 受け入れ基準 | 区分 | 根拠 |
| ------------ | ---- | ---- |
| ログ計測の仕組み自体(起点記録・spawn完了記録・集計) | 自動 | `src-tauri/src/capture/mod.rs`(`format_latency_log`純粋関数、cargo test)・`src/test/latencySummary.test.ts`(`scripts/latency-summary.mjs`のパース・中央値集計をVitestで検証) |
| 中間計測(T11、devビルド、10回試行の中央値<200ms) | 実施済み(手動、記録あり) | `testreport/nfr-001/result-20260923.md`: 約25ms、200ms未満、CLI継続の判断済み |
| **最終計測(T18、リリースビルド、10回試行の中央値<200ms、グローバルショートカット・トレイ経由含む)** | **手動#24〜#26(未実施)** | 本タスクでリリースビルド(`.app`、4.5MB、`src-tauri/target/release/bundle/macos/Tadcap.app`)の生成までは完了。実機での10回試行計測は人間の実施待ち。手順は`testreport/nfr-001/README.md`と`CHECKLIST_T18.md`項目24-26に用意済み |

## NFR-002: 画面収録権限未許可時の案内

| 受け入れ基準 | 区分 | 根拠 |
| ------------ | ---- | ---- |
| ①`screencapture -i`が起動しないこと | 自動(ロジック) + 手動#5 | `capture::run()`が権限未許可時に`screencapture`を起動しない分岐をcargo testで検証済み(TASK§テスト戦略)。実機の実権限状態での確認は手動#5 |
| ②案内UIが表示されること | 自動 + 手動#5 | `src/ui/permissionBanner.test.ts`(3状態の表示分岐、文言に「システム設定を開く」「再起動が必要な場合がある」旨を含むことまで検証)。E2Eでもバナー表示を検証(`capture-flow.spec.ts`の2本目)。実機表示確認は手動#5 |
| ③「システム設定を開く」ボタンで実際にシステム設定アプリの該当画面が開くこと | 手動#6 | `open_screen_recording_settings`コマンドの呼び出し自体は`src/ipc/permissions.test.ts`でモック検証済みだが、実際にシステム設定アプリの該当画面が開くかはOS依存のため手動確認のみ |
| (T08申し送り)権限許可後の反映に再起動が必要な場合がある旨の文言、実機での再起動要否 | 自動(文言) + 手動#7・#8 | `permissionBanner.test.ts::アプリの再起動が必要な場合がある旨の案内を含む`で文言存在は検証済み。実機で実際に再起動が必要かどうかの確認はチェックリスト#7・#8で新規追加(T08申し送り事項) |

## NFR-003: 軽量性

| 受け入れ基準 | 区分 | 根拠 |
| ------------ | ---- | ---- |
| 依存を最小限に保つ(React・Vue等の重量フレームワークを使わない) | レビュー確認 | `package.json`(dependencies: `@tauri-apps/api`, `@tauri-apps/plugin-clipboard-manager`, `@tauri-apps/plugin-opener`のみ。devDependenciesにVitest/Playwright/TypeScript/Vite)・`src-tauri/Cargo.toml`を確認、React/Vue等未使用。`npm run build`出力は`dist/assets/index-*.js 17.76kB(gzip 6.32kB)`・`index-*.css 4.29kB`と軽量。リリースバイナリは4.5MB(ad-hoc署名、`.app`) |

## サマリー: 未カバー項目一覧(要申し送り)

| # | 項目 | 内容 | 対応案 |
| - | ---- | ---- | ------ |
| 1 | FR-005(クリップボード) | Rustフォールバック(`write_image_fallback`/`arboard`)経路を実クリップボードで実地確認する手段がUI上に無い。分岐ロジック自体はモックテストで自動検証済み | 恒久対応が必要なら、デバッグビルド限定で主経路を強制失敗させる環境変数等を追加するタスクを検討(MVPスコープ外、追加要件のためPJM判断が必要) |
| 2 | FR-006(矢印の既定色) | `#FF5C8A`のレンダリング結果を検証する独立した自動/手動チェック項目が無い(コード上の定数値は確認済み) | リスクは低いが、UIリグレッションテスト(スクリーンショット比較等)を追加する場合はT19のUIレビュー資産(`output/reports/ui/`)の枠組みが使える |
| 3 | FR-004(ショートカット変更UI不在) | 「変更UIを提供しない」という否定的要件を継続的に検証する自動/手動項目が無い(現時点でレビューにより不在を確認済み) | 将来のUI変更時にDOM不在テストを追加すると回帰防止になる(現時点では優先度低) |
| 4 | NFR-001 最終計測 | リリースビルドでの10回試行計測(T18)が本レポート作成時点で未実施 | `testreport/manual/CHECKLIST_T18.md`項目24〜26を人間が実機で実行し、`testreport/nfr-001/result-<日付>.md`に追記する必要がある |

上記4件を除く全ての受け入れ基準は「自動テストで確認済み」「手動確認チェックリストの該当項目で確認予定(実施待ち)」のいずれかに対応付けられている。手動確認チェックリスト(28項目)自体は本タスクで実施しておらず(人間による実機操作が必要なため)、全項目の実施・結果記録が完了して初めてT18の受け入れ条件(`output/tasks/TASK_tadcap_mvp.md` T18節)を満たす。
